// SUS.load

export const SUSload = {

    read(slot) {

        return {
            scan: slot.scan,
            vector: slot.vector,
            time: slot.time,
            status: "loaded"
        }
    }
}
