// SUS.list

export const SUSlist = {

    make(link) {

        return {
            items: [
                link.respo,
                link.memory,
                link.vector,
                link.scan,
                link.shifted
            ],
            count: 5,
            status: "list-ready"
        }
    }
}
