// SUS.memory

export const SUSmemory = {

    store(build) {

        const slot = {
            orbit: build.orbit,
            vector: build.vector,
            depth: build.orbit.depth
        }

        return {
            slot,
            status: "memory-stored"
        }
    }
}
