// SUS.save

export const SUSsave = {

    write(scan, shifted) {

        const slot = {
            scan: scan.code,
            vector: shifted,
            time: Date.now()
        }

        return {
            slot,
            status: "saved"
        }
    }
}
