// core/core.dot

export const coreDot = {

    mark(line) {

        return {
            x: line.start,
            y: (line.slope * line.start).toFixed(3),
            status: "core-dot-ready"
        }
    }
}
