// core/core.link

export const coreLink = {

    connect(eye, scale, engine) {

        return {
            eye,
            scale,
            engine,
            status: "core-link-ready"
        }
    }
}
