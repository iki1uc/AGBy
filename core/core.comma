// core/core.comma

export const coreComma = {

    shift(line) {

        const offset = 0.013

        return {
            start: (line.start + offset).toFixed(3),
            end:   (line.end   + offset).toFixed(3),
            slope: line.slope,
            status: "core-comma-ready"
        }
    }
}
